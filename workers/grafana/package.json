{
  "name": "grafana-mobileccs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "postinstall": "wrangler types",
    "check": "wrangler check && tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.123.0"
  }
}
